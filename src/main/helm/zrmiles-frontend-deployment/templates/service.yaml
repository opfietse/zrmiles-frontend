apiVersion: v1
kind: Service
metadata:
  name: {{ include "zrmiles-frontend-deployment.fullname" . }}
  labels:
    {{- include "zrmiles-frontend-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zrmiles-frontend-deployment.selectorLabels" . | nindent 4 }}
